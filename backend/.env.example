# Backend environment variables

PORT=3001
NODE_ENV=development

# SQLite (local dev)
SQLITE_PATH=./data/app.db

# CORS
WEB_ORIGIN=http://localhost:5173

# --- TODO: MongoDB migration ---
# When ready to migrate, replace SQLite with:
# MONGODB_URI=mongodb://localhost:27017/aws-infra-canvas
# Then swap src/db/client.ts and src/db/queries.ts for Mongoose equivalents.
